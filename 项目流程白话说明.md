# 脑 CT 分割与穿刺路径规划项目白话说明

这份文档给完全不懂医学的人看，尽量不用专业话解释这个项目在做什么。

先说结论：这个项目不是在“自动看病”，也不是可以直接替医生做手术决定的软件。它更像一个影像辅助工具原型：把一堆 CT 原始图片整理成三维数据，粗略标出骨头、脑组织、血肿和一些需要避开的危险区域，然后尝试找几条从颅骨外面到血肿位置的直线路径，最后生成图和报告，方便人去检查。

## 一句话理解整个项目

可以把这个项目想成一条流水线：

原始 CT 小图片 -> 拼成三维脑袋模型 -> 标出骨头和血肿 -> 标出大概不能碰的区域 -> 找一条尽量短、尽量直、尽量避开危险区域的穿刺路线 -> 生成图片和报告。

## 项目里主要有什么

项目根目录里主要有这些东西：

- `头模CT/`：原始 CT 数据。里面是一张张 DICOM 文件，可以理解成医院机器导出的原始影像小切片。
- `scripts/`：所有处理脚本都在这里。
- `scripts/README.md`：比较技术化的运行说明。
- `output_nifti/`：运行后才会生成的输出目录，里面会放转换后的三维影像、分割结果、路径结果和报告。

## 先理解几个词

### DICOM 是什么

DICOM 是医院影像设备常用的原始文件格式。CT、MRI 拍出来通常不是一张普通 PNG/JPG，而是一组 DICOM 文件。每个文件除了图像，还有患者、扫描参数、空间位置等信息。

### NIfTI 是什么

NIfTI 是医学影像分析里常用的三维数据格式，文件后缀一般是 `.nii.gz`。

如果 DICOM 像“一堆切片照片”，NIfTI 就像“把这些切片按正确顺序叠起来后的三维模型”。

### mask 是什么

mask 可以理解成“涂色图层”。

比如原始 CT 是黑白图，项目会额外生成一个颅骨 mask：骨头的位置标成 1，其他地方标成 0。这样程序就知道哪里是骨头。

同理还有：

- 血肿 mask：哪里可能是血肿。
- 脑室 mask：哪里可能是脑室。
- 血管风险 mask：哪里尽量不要穿过。
- 功能区 mask：哪里尽量不要碰。

### HU 值是什么

CT 图像里的每个点都有一个数字，叫 HU 值。不同组织在 CT 上的数值范围大致不同。

简单理解：

- 空气很低。
- 水和脑脊液偏低。
- 普通脑组织在一个中间范围。
- 新鲜出血通常更亮，HU 值更高。
- 骨头非常亮，HU 值更高。

这个项目大量使用 HU 阈值，也就是“某个数字范围内的点，大概率属于某类组织”。

这是一种简单办法，但不是百分百准确。

## 步骤 1：DICOM 转 NIfTI

对应脚本：`scripts/dicom_to_nifti.py`

### 它在做什么

它会扫描 `头模CT/` 里的所有 DICOM 文件，把同一组 CT 切片找出来，然后拼成一个三维影像文件。

可以理解成：

一堆散落的 CT 切片 -> 按拍摄顺序排好 -> 叠成一个三维脑袋 CT。

### 它会跳过什么

CT 数据里可能混着一些不是正式三维扫描的东西，比如：

- 定位像：拍 CT 前用来定位的大致图。
- 剂量报告：机器记录辐射剂量的报告图。
- 切片太少的序列：无法构成可靠三维体数据。

这些会被自动跳过。

### 产物是什么

运行后会生成：

- `.nii.gz`：三维 CT 文件。
- `_preview.png`：三视图预览图，方便肉眼检查。
- `manifest.csv`：记录哪些序列转换成功、哪些被跳过。

## 步骤 2：颅骨分割

对应脚本：`scripts/skull_segmentation.py`

### 它在做什么

它从 CT 里找骨头。

骨头在 CT 上很亮，HU 值高，所以程序会用一个大致范围，比如 300 到 3000 HU，把疑似骨头的地方筛出来。

然后它会做一些清理：

- 去掉零散小噪点。
- 保留最大的一整块骨头结构。
- 尽量填补骨缝或小断裂。

### 为什么要先找骨头

因为后面很多事情都要知道“颅骨在哪里”：

- 从骨头内部推测颅腔范围。
- 路径规划时，从颅骨表面挑入颅点。
- 判断路径是不是穿了太厚的骨头。

### 产物是什么

- `_skull_mask.nii.gz`：颅骨标记图层。
- `_skull_overlay.png`：CT 上叠加骨头颜色。
- `_skull_3d.png`：颅骨三维示意图。
- `_skull_stats.txt`：骨头体积、体素数量等统计。

## 步骤 3：颅腔、脑组织、血肿粗分割

对应脚本：`scripts/brain_hematoma_segmentation.py`

这一步比较核心。

### 3.1 找颅腔

颅腔就是颅骨里面装脑子的空间。

程序先拿到颅骨 mask，然后做“填洞”：

把骨头围起来的内部区域填出来，再减掉骨头本身，剩下的就是大致的颅腔。

白话理解：

先找到头骨壳 -> 把壳里面的空间涂出来 -> 这个空间就是脑袋内部区域。

### 3.2 找脑组织

在颅腔里面，程序再用 HU 范围筛选脑组织。

它大致认为 HU 在 0 到 80 之间的部分是脑组织或类似软组织。

这一步会包含血肿，因为血肿也在这个范围附近，只是通常更亮。

### 3.3 找血肿

血肿在 CT 上通常比普通脑组织亮一些。项目用大概 45 到 80 HU 这个范围筛选疑似血肿。

但只靠 HU 会有误判，比如钙化、骨头边缘伪影、脉络丛等也可能比较亮。所以程序又加了几个条件：

- 体积太小的不要。
- 离颅骨太近的不要，减少骨头边缘误判。
- 形状太散的不要。
- 做形态学开运算，把细小连接断开。

### 这一步可靠吗

只能说是“粗分割”。它可能漏掉血肿，也可能把其他高密度结构误认为血肿。真实医学使用需要医生复核，最好还要有标注数据验证。

### 产物是什么

- `_intracranial_mask.nii.gz`：颅腔。
- `_brain_mask.nii.gz`：脑组织。
- `_hematoma_mask.nii.gz`：疑似血肿。
- `_brain_overlay.png`：把这些区域叠加到 CT 上。
- `_brain_3d.png`：颅骨和血肿三维图。
- `_brain_report.json`：体积、位置、平均 HU 等数据。

## 步骤 4：脑室分割

对应脚本：`scripts/ventricle_segmentation.py`

### 脑室是什么

脑室可以简单理解成脑子里面装脑脊液的空腔。它里面主要是液体，在 CT 上数值和脑组织不一样。

### 它在做什么

程序在颅腔里面找 HU 比较低、像脑脊液的区域。然后再排除靠近颅骨的部分，因为靠近颅骨的低密度区域可能是脑沟或其他空间，不一定是脑室。

### 为什么路径规划要知道脑室

穿刺路径如果穿过脑室，可能增加风险或不符合预期，所以项目把脑室作为需要避开的区域之一。

### 产物是什么

- `_ventricle_mask.nii.gz`：脑室标记。
- `_ventricle_overlay.png`：脑室叠加图。
- `_ventricle_3d.png`：脑室三维图。
- `_ventricle_stats.json`：脑室体积等统计。

## 步骤 5：血管风险禁区

对应脚本：`scripts/vessel_risk_segmentation.py`

### 关键点

普通平扫 CT 通常看不清很多血管。真正要看血管，通常需要 CTA、MRA 或增强影像。

所以这个脚本不是在“真实分割血管”，而是在做“保守避让区”。

### 它在做什么

它用两类办法画出不建议穿过的区域：

1. 找 CT 里很亮的高密度结构，比如钙化区域。
2. 根据大致解剖位置，画出一些经验区域，比如：
   - 头顶中线附近的上矢状窦区域。
   - 中线附近的大脑镰区域。
   - 颅底附近的血管风险区域。

### 为什么叫风险禁区

因为它不是精确血管图，只是提醒路径规划：“这些地方可能危险，尽量绕开。”

### 产物是什么

- `_vessel_risk_mask.nii.gz`：血管风险禁区。
- `_vessel_overlay.png`：风险区叠加图。
- `_vessel_3d.png`：风险区三维图。
- `_vessel_stats.json`：各类风险区体积统计。

## 步骤 6：脑干分割

对应脚本：`scripts/brainstem_segmentation.py`

### 脑干是什么

脑干是非常重要的脑部结构，控制呼吸、心跳等基础生命功能。手术路径一般要非常谨慎地避开。

### 关键点

平扫 CT 上，脑干和周围脑组织的灰度很接近，程序很难直接看出来。

所以这个脚本也不是精确分割脑干，而是根据大致位置估算一个“脑干禁区”。

### 它在做什么

程序会在颅腔下部、中线附近、前后居中的区域里，结合 HU 范围，估算一个脑干区域。

白话理解：

脑干大概在脑袋里面偏下、偏中间的位置，所以程序在这个区域画一个比较保守的避让区。

### 产物是什么

- `_brainstem_mask.nii.gz`：脑干近似禁区。
- `_brainstem_overlay.png`：脑干叠加图。
- `_brainstem_3d.png`：脑干三维图。
- `_brainstem_stats.json`：体积和位置统计。

## 步骤 7：功能区几何近似

对应脚本：`scripts/eloquent_zone_segmentation.py`

### 功能区是什么

脑功能区是指负责运动、语言、视觉等重要功能的区域。比如运动区受伤可能影响肢体活动，语言区受伤可能影响说话。

### 关键点

真正的功能区定位，通常需要 MRI、功能 MRI 或图谱配准。单靠 CT 很难准确知道每个人的功能区在哪里。

所以这个脚本不是在真实识别功能区，而是按经验位置画出大概需要避开的区域。

### 它在做什么

程序会根据颅腔的大小，把脑袋内部坐标归一化，然后按大致比例画出 4 类禁区：

- 运动区：大概在靠近头顶、偏中间的位置。
- 语言区：按经验放在左半球部分区域。
- 视觉区：大概在后脑勺方向。
- 深部核团：大概在脑部较深、靠近中线的区域。

### 产物是什么

- `_eloquent_zone_mask.nii.gz`：所有功能区禁区合并版。
- `_eloquent_motor_mask.nii.gz`：运动区。
- `_eloquent_language_mask.nii.gz`：语言区。
- `_eloquent_visual_mask.nii.gz`：视觉区。
- `_eloquent_deep_mask.nii.gz`：深部核团。
- `_eloquent_overlay.png`：功能区叠加图。
- `_eloquent_3d.png`：功能区三维图。
- `_eloquent_stats.json`：各区域体积统计。

## 步骤 8：穿刺路径规划

对应脚本：`scripts/path_planning.py`

这是最终核心步骤。

### 它想解决什么问题

如果血肿在脑子里，医生可能需要从颅骨外面找一个进入点，把穿刺针或引流管送到血肿位置。

这个脚本尝试自动找几条候选直线路径。

### 它怎么找路径

可以分成几件事：

#### 1. 找候选入颅点

程序会从颅骨外表面采样很多点，作为可能的钻孔入口。

它会尽量避开脸部和颅底，只保留相对适合入颅的上半部分颅骨区域。

#### 2. 找目标点

目标点不是只取血肿中心一个点。

程序会看血肿的长轴方向，然后沿着这个方向取多个点，比如：

- 血肿一端。
- 血肿中心。
- 血肿另一端。

这样做是为了让路径不仅能到中心，也能覆盖血肿长条形区域。

#### 3. 检查每条直线能不能用

程序会把每个入颅点和每个目标点连成一条直线，然后沿线逐点检查：

- 有没有出界。
- 进颅时穿过的骨头是不是太厚。
- 进入颅腔后有没有再次碰到骨头。
- 路径大部分是不是在颅腔内。
- 有没有穿过脑室。
- 有没有穿过血管风险区。
- 有没有穿过脑干禁区。
- 有没有穿过功能区禁区。

只要违反规则，这条路径就被丢掉。

#### 4. 给剩下路径打分

合法路径可能有很多，程序会按两个主要指标排序：

- 路径越短越好。
- 入颅角度越接近合适方向越好。

最后输出 Top-N 条候选路径。

### 产物是什么

- `_paths.json`：所有候选路径的详细数据，包括入颅点、目标点、长度、角度、评分。
- `_paths_overlay.png`：在 CT 三视图上画出路径。
- `_paths_3d.png`：三维图里显示路径。

## 步骤 9：自然语言方案和 PDF 报告

对应脚本：

- `scripts/narrative.py`
- `scripts/case_report.py`

### 它在做什么

前面的步骤会生成很多技术文件，比如 JSON、mask、PNG。这个步骤把它们整理成人更容易看的形式。

它会生成：

- `_plan.txt`：中文文字版手术路径建议。
- `_report.pdf`：PDF 报告，包含 CT 预览、分割图、路径图、统计表等。

### 注意

这里的“手术方案”只是自动生成的参考文字，不是医生最终方案。

它根据路径 JSON 里的入颅点、目标点、长度、角度等信息，翻译成中文描述。

## 辅助脚本：修复乱码文件名

对应脚本：`scripts/rename_legacy_garbled.py`

这个脚本和医学处理关系不大。

它主要解决以前生成文件时中文路径或中文序列名出现乱码的问题。比如把错误显示的“脑部”乱码替换成 `brain`。

## 最终输出长什么样

如果全流程成功，一个病例目录里可能会有：

- 原始转换后的 CT：`CT_brain_...nii.gz`
- 各类 mask：骨头、脑、血肿、脑室、风险区等。
- 各类预览图：`overlay.png`、`3d.png`
- 路径结果：`paths.json`、`paths_overlay.png`、`paths_3d.png`
- 报告：`plan.txt`、`report.pdf`

简单说：

`.nii.gz` 是给软件继续分析用的三维数据。

`.png` 是给人快速看效果的图片。

`.json` 是给程序或技术人员看的结构化结果。

`.pdf` 和 `.txt` 是给人阅读的报告。

## 这个项目最重要的限制

### 1. 它不是 AI 训练模型

当前项目主要靠规则、阈值和几何推断，不是深度学习模型。

优点是不用训练，容易跑。

缺点是准确性受规则影响很大。

### 2. CT 上很多东西看不清

平扫 CT 对骨头和明显出血比较友好，但对血管、脑干边界、功能区并不友好。

所以这些区域目前很多是“估算禁区”，不是精确分割。

### 3. 血肿分割可能误判

血肿一般比较亮，但钙化、骨头边缘、图像噪声也可能亮。

所以血肿 mask 必须人工检查。

### 4. 路径规划只考虑直线

程序找的是直线路径，没有真实模拟手术器械、皮肤、肌肉、骨缝、血管变异等复杂情况。

### 5. 不能直接用于临床

所有输出都应该被看作辅助分析结果，需要医生和影像专家复核。

## 如果给外行人讲，可以这么说

这个项目做的事大概是：

先把医院 CT 的一堆切片拼成一个三维脑袋模型。然后用图像亮度和一些几何规则，把骨头、脑组织、疑似出血区域标出来。接着再画出一些最好不要碰的区域，比如脑室、血管风险区、脑干和功能区。最后，程序从头骨表面尝试找几条能到达血肿、又尽量不穿过危险区域的直线，并生成图片和报告。

但它只是一个原型工具。它能帮人快速做初步分析，不能替代医生判断，也不能直接当作手术导航。

## 建议下一步

如果要继续推进，建议按这个顺序：

1. 先把依赖环境装好，确保当前 2 个病例能完整跑通。
2. 跑完后先看每一步的 PNG 叠加图，确认分割有没有明显错误。
3. 用 3D Slicer 打开 `.nii.gz` 和 mask，人工复核三维位置。
4. 如果要提高准确性，需要医生标注数据，或者接入 MRI、CTA/MRA 等更合适的影像。
5. 在没有人工复核前，不要把生成的路径当作真实手术建议。
